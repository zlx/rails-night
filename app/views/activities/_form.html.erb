<%= simple_form_for @activity, html: {class: 'form-horizontal'} do |f| %>
  <%= f.error_notification %>

<div> 
  <%= f.input :title, input_html: {class: 'form-control'} %>
  <%= f.association :author, collection: User.all.map{|u| [u.nickname, u.id]} , input_html: {class: 'form-control'}%>
  <%= f.input :description, input_html: {class: 'span6 form-control', size: '500x10'}, hint: t('common.markdown') %>
  <%= f.input :status, collection: Activity::STATUS.map{|k, v| [v, k]}, input_html: {class: 'form-control'} %>
  <%= f.input :start_at, as: :string, input_html: { value: @activity.start_at } , input_html: {class: 'form-control'}%>
  <%= f.input :summary, input_html: {class: 'span6', size: '500x10'}, hint: t('common.markdown'), input_html: {class: 'form-control'} %>
</div>

<div class="form-groups">
  <div class="col-lg-offset-2 col-lg-10">
  <%= f.button :submit, class: 'btn btn-primary' %>
</div>
</div>
<% end %>
